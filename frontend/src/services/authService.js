import { getApiErrorMessage, loginUser, loginWithGoogle, registerUser } from "./api";

export const authService = {
  getApiErrorMessage,
  loginUser,
  loginWithGoogle,
  registerUser
};

export { getApiErrorMessage, loginUser, loginWithGoogle, registerUser };
